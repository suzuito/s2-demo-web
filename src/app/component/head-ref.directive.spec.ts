import { HeadRefDirective } from './head-ref.directive';

describe('HeadRefDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadRefDirective();
    expect(directive).toBeTruthy();
  });
});
